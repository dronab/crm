extends layout

block content
    if(message)
        p=message
    .container
        h1=title
        form(method='post')
            div(class='input-field col s6')
                input#login(type='text', name='login' required=true, class='validate')
                label(for='login') Имя пользователя
            div(class='input-field col s6')
                input#password(type='password', name='password' required=true, class='validate')
                label(for='password') Пароль
            button(class='waves-effect waves-light btn') Зарегестрировать пользователя
        a(href='/') Войти в систему